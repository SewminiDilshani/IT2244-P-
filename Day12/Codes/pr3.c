#include<stdio.h>                            // Includes the standard input/output library
int main() {
    int firNum = 25;                         // Declare and initialize an integer variable 'firNum' with the value 25
    printf("First number is: %d\n", firNum); // Print the value of 'firNum'
    int secoNum = firNum;                    // Declare another variable 'secoNum' and assign it the value of 'firNum'
    printf("Second Number is: %d\n", secoNum); // Print the value of 'secoNum'
    return 0;                                // Return 0 to indicate successful execution
}
